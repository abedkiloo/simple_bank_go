version: "2"
servers: [ ]
sql:
  - engine: "postgresql"
    schema: "db/migration/"
    queries: "db/query/"
    gen:
      go:
        package: "db"
        out: "internal/db/sqlc/"

plugins: [ ]

rules: [ ]

options: { }
